OBJECTS = Profiles.o ArDef.o main.o Flux.o ProfilesArrays.o ProfilesToFiles.o \
          Q.o Equation.o Bootstrap.o Evans60.o

main.exe: $(OBJECTS)
	gfortran $(OBJECTS) -o main.exe
	./main.exe
	#gnuplot graphics.graph
	python graph.py

%.o : %.f90
	gfortran -c $<

clean:
	rm -f $(OBJECTS)

